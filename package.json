{
  "name": "jest-regexp-sample",
  "version": "1.0.0",
  "description": "regexp sample to test with tags",
  "author": "Takagi Kensuke <janus.wel.3@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "npm run mocha:testWithout:freaky",
    "jest:test": "jest",
    "jest:testWith:freaky": "jest --testNamePattern '\\[freaky]'",
    "jest:testWithout:freaky": "jest --testNamePattern '^(?!.*\\[freaky]).*$'",
    "jest:test:smoke": "jest --testNamePattern '\\[smoke]'",
    "mocha:test": "nyc mocha **/*.test.ts",
    "mocha:testWith:freaky": "nyc mocha **/*.test.ts --fgrep '[freaky]'",
    "mocha:testWithout:freaky": "nyc mocha **/*.test.ts --fgrep '[freaky]' --invert",
    "mocha:test:smoke": "nyc mocha **/*.test.ts --fgrep '[smoke]'",
    "node:test": "node --require ts-node/register --test **/node.test.ts",
    "node:testWith:freaky": "node --require ts-node/register  --test-name-pattern '\\[freaky]' --test **/node.test.ts",
    "node:testWithout:freaky": "echo NOT WORKS # node --require ts-node/register  --test-name-pattern '^(?!.*\\[freaky]).*$' --test **/node.test.ts",
    "node:test:smoke": "node --require ts-node/register  --test-name-pattern '\\[smoke]' --test **/node.test.ts",
    "verify": "concurrently npm:verify:*",
    "verify:type": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "mocha": "^10.4.0",
    "nyc": "^15.1.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
